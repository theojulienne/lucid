The Lucid Toolkit uses the cross-platform CBuild build system.
Prebuilt binaries are included in the distribution.

To compile:

	Bourne-compatible shell:
	./compile.sh

	Microsoft Windows:
	compile.bat

